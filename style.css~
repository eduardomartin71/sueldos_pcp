*{
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

:root{
    --colorLetra: #111;
    --branco: #fff;
}

html, body{
  font-family: verdana, sans-serif;
  color: var(--colorLetra);
}

/*ESTILO DEL CALENDARIO*/

.calendar{
border: 2px solid darkGray;
  position: relative;
  width: 300px;
  height: 300px;
background-color: lightGrey;
}

.calendar .titulo{
  margin: 5px 0px;
  text-align: center;
  font-size: 1.2em;
  font-weight: 660;
}

.calendar .mesAno{
  display: flex;
  text-align: center;
  justify-content: : space-between;
}

.calendar .mesAno .arrows{
  font-size: 1.2em;
  font-weight: 660;
}
.calendar .mesAno .arrows:hover{
  font-size: 1.3em;
  font-weight: 700;
  cursor: default;
}
